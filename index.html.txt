```html
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>りつナビ - 立命館大学新歓情報</title>
    <!-- Tailwind CSS & Lucide Icons -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
    </style>
</head>
<body class="bg-gray-100">
    <div id="root"></div>

    <!-- React & Babel -->
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

    <script type="text/babel">
        const { useState, useMemo, useEffect } = React;

        const App = () => {
            const [view, setView] = useState('list');
            const [selectedEvent, setSelectedEvent] = useState(null);
            const [filterGenre, setFilterGenre] = useState('すべて');
            const [searchQuery, setSearchQuery] = useState('');
            const [isAdmin, setIsAdmin] = useState(false);
            const [showLinkModal, setShowLinkModal] = useState(false);

            // アイコンを読み込むための処理
            useEffect(() => {
                lucide.createIcons();
            }, [view, showLinkModal]);

            const [events, setEvents] = useState([
                {
                    id: 1,
                    circleName: "立命館大学 広告研究会",
                    eventName: "春の新歓映像祭 & 交流会",
                    date: "2026-04-10",
                    time: "18:00 - 20:00",
                    place: "衣笠キャンパス 至善館 1F",
                    price: "無料",
                    genre: "文化",
                    tags: ["初心者歓迎", "映像制作"],
                    description: "広告研究会では、CM制作やグラフィックデザインを行っています。新歓では過去の作品上映と、先輩たちとのフリートークを楽しめます！",
                    frequency: "週2回（火・金）",
                    atmosphere: "クリエイティブで落ち着いた雰囲気です。",
                    linkUrl: "https://www.instagram.com/",
                    linkType: "Instagram",
                    qanda: [{ q: "カメラは必要？", a: "貸出用があります！" }]
                },
                {
                    id: 2,
                    circleName: "BKCテニスサークル R-Shot",
                    eventName: "体験練習会 @テニスコート",
                    date: "2026-04-12",
                    time: "13:00 - 16:00",
                    place: "BKC テニスコート第3",
                    price: "500円",
                    genre: "スポーツ",
                    tags: ["初心者歓迎", "手ぶらOK"],
                    description: "BKC最大級のテニスサークルです！経験者はもちろん、大学からテニスを始めたい人も大歓迎です。",
                    frequency: "週3回（月・水・土）",
                    atmosphere: "とにかく明るく元気なサークルです。",
                    linkUrl: "https://line.me/",
                    linkType: "公式LINE",
                    qanda: [{ q: "ラケットは？", a: "貸出あります！" }]
                }
            ]);

            const filteredEvents = useMemo(() => {
                return events
                    .filter(ev => filterGenre === 'すべて' || ev.genre === filterGenre)
                    .filter(ev => ev.circleName.includes(searchQuery) || ev.eventName.includes(searchQuery))
                    .sort((a, b) => new Date(a.date) - new Date(b.date));
            }, [events, filterGenre, searchQuery]);

            const handleOpenLink = () => {
                if (selectedEvent?.linkUrl) {
                    window.open(selectedEvent.linkUrl, '_blank');
                    setShowLinkModal(false);
                }
            };

            const Header = () => (
                <div className="sticky top-0 z-20 bg-white border-b border-gray-100 px-4 py-3 flex justify-between items-center">
                    <div className="flex items-center gap-2">
                        <div className="w-8 h-8 bg-green-800 rounded-lg flex items-center justify-center">
                            <span className="text-white font-bold text-xs">R</span>
                        </div>
                        <h1 className="text-xl font-black text-gray-800 tracking-tight">りつナビ</h1>
                    </div>
                    <button 
                        onClick={() => setIsAdmin(!isAdmin)}
                        className={`flex items-center gap-1 px-3 py-1.5 rounded-full text-[10px] font-black transition-all ${isAdmin ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-600'}`}
                    >
                        {isAdmin ? 'サークル管理中' : '管理者ログイン'}
                    </button>
                </div>
            );

            return (
                <div className="max-w-md mx-auto min-h-screen bg-gray-50 flex flex-col relative shadow-2xl overflow-x-hidden">
                    <Header />
                    
                    {view === 'list' && (
                        <div className="flex-1 overflow-y-auto">
                            <div className="p-4 bg-white border-b border-gray-100">
                                <input 
                                    type="text"
                                    placeholder="サークルやイベントを検索"
                                    className="w-full bg-gray-100 rounded-xl py-2.5 px-4 text-sm focus:outline-none mb-4"
                                    value={searchQuery}
                                    onChange={(e) => setSearchQuery(e.target.value)}
                                />
                                <div className="flex gap-2 overflow-x-auto no-scrollbar pb-1">
                                    {['すべて', 'スポーツ', '文化', '音楽', 'ボランティア'].map(g => (
                                        <button
                                            key={g}
                                            onClick={() => setFilterGenre(g)}
                                            className={`px-4 py-1.5 rounded-full text-xs font-bold whitespace-nowrap border ${filterGenre === g ? 'bg-green-800 border-green-800 text-white' : 'bg-white border-gray-200 text-gray-500'}`}
                                        >
                                            {g}
                                        </button>
                                    ))}
                                </div>
                            </div>
                            <div className="p-4 space-y-4 pb-20">
                                {filteredEvents.map(event => (
                                    <div key={event.id} className="bg-white rounded-[32px] border border-gray-100 overflow-hidden shadow-sm" onClick={() => { setSelectedEvent(event); setView('detail'); }}>
                                        <div className="h-40 bg-green-50 flex items-center justify-center relative">
                                            <span className="text-green-800/10 font-black text-6xl">{event.circleName[0]}</span>
                                            <div className="absolute bottom-3 left-3 bg-white/90 px-3 py-1 rounded-full text-[10px] font-bold text-gray-700 shadow-sm">
                                                {event.date.substring(5)}
                                            </div>
                                        </div>
                                        <div className="p-5">
                                            <p className="text-[9px] font-black text-green-800 uppercase">{event.circleName}</p>
                                            <h3 className="text-lg font-black text-gray-800 leading-tight mb-2">{event.eventName}</h3>
                                            <p className="text-[11px] text-gray-500">{event.place}</p>
                                        </div>
                                    </div>
                                ))}
                            </div>
                        </div>
                    )}

                    {view === 'detail' && selectedEvent && (
                        <div className="flex-1 bg-white pb-24">
                            <div className="relative h-48 bg-green-900 flex items-center justify-center">
                                <button onClick={() => setView('list')} className="absolute top-4 left-4 bg-white p-2 rounded-full shadow-lg font-bold">←</button>
                                <span className="text-white/10 font-black text-8xl">{selectedEvent.circleName[0]}</span>
                            </div>
                            <div className="p-8 -mt-10 bg-white rounded-t-[40px] shadow-2xl">
                                <h2 className="text-2xl font-black text-gray-800 mb-1">{selectedEvent.eventName}</h2>
                                <p className="text-green-800 font-black text-sm mb-6">{selectedEvent.circleName}</p>
                                <div className="grid grid-cols-2 gap-3 mb-8">
                                    <div className="bg-gray-50 p-3 rounded-2xl">
                                        <p className="text-[8px] font-black text-gray-400">DATE</p>
                                        <p className="text-[11px] font-black">{selectedEvent.date}</p>
                                    </div>
                                    <div className="bg-gray-50 p-3 rounded-2xl">
                                        <p className="text-[8px] font-black text-gray-400">PLACE</p>
                                        <p className="text-[11px] font-black">{selectedEvent.place}</p>
                                    </div>
                                </div>
                                <p className="text-sm text-gray-600 mb-8">{selectedEvent.description}</p>
                                <button 
                                    onClick={() => setShowLinkModal(true)}
                                    className="w-full bg-green-800 text-white py-4 rounded-[24px] font-black text-lg shadow-xl shadow-green-900/30"
                                >
  